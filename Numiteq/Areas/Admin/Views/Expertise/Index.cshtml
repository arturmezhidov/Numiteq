@model Numiteq.Areas.Admin.ViewModels.Expertises.IndexViewModel
@{
    ViewData["Title"] = "Expertises";
    int i = 0;
}

<h2>@ViewData["Title"]</h2>

<a asp-action="Add" class="">Add</a>

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var expertise in Model.Expertises)
        {
            <tr>
                <th scope="row">@i</th>
                <td>@expertise.Title</td>
                <td>@Html.Raw(expertise.Description)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@expertise.Id">Edit</a>
                    | <a asp-action="Delete" asp-route-id="@expertise.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>