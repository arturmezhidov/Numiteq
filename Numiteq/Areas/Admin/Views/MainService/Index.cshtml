@model Numiteq.Areas.Admin.ViewModels.MainServices.IndexViewModel
@{
    ViewData["Title"] = "Main Services";
    int i = 0;
}

<h2>@ViewData["Title"]</h2>

<a asp-action="Add" class="">Add</a>

<table class="table">
    <thead class="thead-dark">
        <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var service in Model.MainServices)
        {
            <tr>
                <th scope="row">@i</th>
                <td>@service.Title</td>
                <td>@service.Description</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@service.Id">Edit</a>
                    | <a asp-action="Delete" asp-route-id="@service.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>